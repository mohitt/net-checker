[project]
name = "net-checker"
version = "0.1.0"
description = "Watch the internet connection and report how long it was down."
readme = "README.md"
authors = [
    { name = "Mohit Thakral", email = "me@mohitt.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
net-checker = "net_checker:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
